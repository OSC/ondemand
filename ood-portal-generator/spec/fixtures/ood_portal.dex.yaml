---
dex:
  tls_cert: dne.crt
  tls_key: dne.key
  storage:
    type: mysql
    config:
      database: dex
      user: dex
      password: secret
  frontend:
    theme: "custom-test-theme"
    dir: /etc/site/custom-dex-themes
    extra:
      loginButtonText: "Test login Text"

