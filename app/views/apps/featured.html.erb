<div class="row">
  <div class="col-md-9">
    <%= render partial: 'shared/welcome' %>

    <% if @nav_groups.any? %>
    <p class="lead"><%= t('dashboard.sharing.welcome_msg_tag').html_safe %></p>
    <% end %>
    <%= render partial: "catalog_link" %>

    <%= render(partial: "group", collection: @groups) || t('dashboard.sharing.no_shared_apps_tag').html_safe %>
  </div>
  <div class="col-md-3">
    <%# This assumes @motd_file responds to `to_partial_path` %>
    <%= render @motd if @motd %>
  </div>
</div>
