<%= render 'header' %>

<%= render 'form_from_rails_template', form_url: create_from_rails_template_path(type: @type, new_method: @new_method) %>

<hr />

<%= link_to 'Back', products_path(type: @type), class: 'btn btn-default' %>
