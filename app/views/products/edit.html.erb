<h1>Editing Product</h1>

<%= render 'form', form_url: product_path(@product.name, type: @type) %>

<%= link_to 'Show', product_path(@product.name, type: @type) %> |
<%= link_to 'Back', products_path(type: @type) %>
