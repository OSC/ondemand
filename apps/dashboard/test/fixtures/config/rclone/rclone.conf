[s3]
type = s3
provider = Other
env_auth = false
access_key_id = 
secret_access_key = 
acl = private

# S3 always tried to query endpoint, use swift for testing invalid remotes.
[missing_auth]
type = swift
env_auth = false